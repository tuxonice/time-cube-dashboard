{% extends "layout.twig" %}

{% set active_page = 'projects' %}

{% block title %}Edit Task — Time Cube{% endblock %}

{% block breadcrumb %}
<nav aria-label="breadcrumb">
    <ol class="breadcrumb mb-0 small">
        <li class="breadcrumb-item"><a href="/projects" class="text-decoration-none">Projects</a></li>
        <li class="breadcrumb-item"><a href="/projects/{{ task.project_id }}/tasks" class="text-decoration-none">{{ task.project_name }}</a></li>
        <li class="breadcrumb-item active">Edit</li>
    </ol>
</nav>
{% endblock %}

{% block content %}
<h1 class="mt-4 mb-4">Edit Task</h1>

<div class="card shadow-sm">
    <div class="card-body">
        <form method="post" action="/tasks/{{ task.id }}">
            <div class="mb-3">
                <label class="form-label" for="name">Name</label>
                <input type="text" id="name" name="name" class="form-control" value="{{ task.name }}" required autofocus>
            </div>
            <div class="mb-4">
                <label class="form-label" for="description">Description</label>
                <textarea id="description" name="description" class="form-control" rows="3">{{ task.description }}</textarea>
            </div>
            <button type="submit" class="btn btn-dark">
                <i aria-hidden="true" class="bi bi-check-lg me-1"></i> Update Task
            </button>
            <a href="/projects/{{ task.project_id }}/tasks" class="btn btn-outline-secondary ms-2">Cancel</a>
        </form>
    </div>
</div>
{% endblock %}
